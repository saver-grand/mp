{
  "name": "restream-server",
  "version": "1.0.0",
  "description": "Node.js server to restream DASH (.mpd) as HLS (.m3u8) using FFmpeg",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
